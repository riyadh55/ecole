

{% block body -%}
    <h1>Matiere</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libellematiere</th>
                <td>{{ entity.libelleMatiere }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('matiere') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('matiere_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
